services:
  voice-agent:
    build: ./backend
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./backend:/app # For development/hot-reload (remove in prod)
    network_mode: host # Recommended for WebRTC/Voice low latency
